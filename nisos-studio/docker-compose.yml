services:
  nisos-agent:
    build:
      context: .
      dockerfile: nisos-studio/Dockerfile
    ports:
      - "8001:8000"
    environment:
      - MOCKOON_URL=http://mockoon:3001
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - LANGSMITH_TRACING=true
      - LANGSMITH_PROJECT=${LANGSMITH_PROJECT}
      - LANGSMITH_API_KEY=${LANGSMITH_API_KEY}
      - LANGSMITH_ENDPOINT=${LANGSMITH_ENDPOINT}
    depends_on:
      - mockoon
    labels:
      # Use the native Coolify domain label (handles routing and SSL automatically)
      - "coolify.fqdn=https://nisos.mstewart.dev"
      # Define the Basic Auth middleware
      - "traefik.http.middlewares.nisos-auth.basicauth.users=user:$$2y$$05$$vbrV9N7Lz583DxZQHxx49uxbU87.5BAXMKeIluEsFqnOrCELK0GIu"
      # Tell Coolify to attach this middleware to the domain above
      - "coolify.traefik.middlewares=nisos-auth"

  mockoon:
    image: mockoon/cli:latest
    ports:
      - "3001:3001"
    volumes:
      - ./mockoon-data.json:/data/mockoon-data.json:ro
    command: ["--data", "/data/mockoon-data.json", "--port", "3001", "--hostname", "0.0.0.0"]
    labels:
      - "coolify.fqdn=https://socialapi.mstewart.dev"
